const styles = (theme) => ({
  inline: {
    verticalAlign: "middle",
    display: "inline-block",
    width: "calc(100% - 50px)",
    color: "#AFAFBE",
    fontFamily:'poppins',
    fontSize: '1rem',
    fontWeight: 400,
  },
});

export default styles;
